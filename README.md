# template-base-fastapi
Construcciones rápidas para backend con Fastapi.

# Comandos utilizados
1. Genera migraciones de los modelos `$ alembic revision --autogenerate -m "Initial migration"`.
2. Aplica las migraciones en la base de datos `$ alembic upgrade head"`.